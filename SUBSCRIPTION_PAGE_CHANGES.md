# صفحهٔ سابسکریپشن پروداکشن RVG

این نسخه رابط عمومی سابسکریپشن RVG را با الگوی عملکردی 3x-ui بازطراحی می‌کند. کاربر با بازکردن لینک عادی گروه، صفحه‌ای واکنش‌گرا با اطلاعات شناسه و وضعیت، جدول ترافیک، سهمیه و انقضا، نوار مصرف، QR، کپی لینک‌ها و منوهای Android و iOS می‌بیند. رابط به‌صورت پیش‌فرض انگلیسی است و از منوی زبان، فارسی نیز پشتیبانی می‌کند. تم روشن و تاریک در مرورگر ذخیره می‌شود.

| وضعیت فراخوانی | رفتار |
| --- | --- |
| مرورگر با لینک تک‌کاربره `https://YOUR_DOMAIN/sub/UUID` | نمایش صفحهٔ عمومی و دریافت جزئیات از `/api/public/single/UUID` |
| مرورگر با لینک گروه `https://YOUR_DOMAIN/sub-group/UUID` | نمایش صفحهٔ عمومی و دریافت جزئیات از `/api/public/sub/UUID` |
| کلاینت‌های VPN شناخته‌شده | بازگرداندن feed استاندارد Base64 و هدرهای سابسکریپشن در هر دو مسیر |
| نیاز به feed خام در ابزار عمومی | افزودن `?view=raw` به URL |
| سابسکریپشن رمزدار در مرورگر | نمایش فرم رمز و سپس بارگذاری اطلاعات |
| سابسکریپشن رمزدار در کلاینت | استفاده از پارامتر موجود `?pw=...` مانند قبل |

## تغییرات فنی

| فایل | تغییر |
| --- | --- |
| `subscription_page.py` | ماژول مستقل رابط HTML/CSS/JavaScript پروداکشن، بدون وابستگی frontend اضافی |
| `main.py` | تشخیص browser/client روی هر دو مسیر `/sub/{uuid}` و `/sub-group/{uuid}`، API عمومی لینک تکی، توسعهٔ دادهٔ عمومی و افزودن تاریخ انقضای هر کانفیگ |
| `pages.py` | wrapper سازگاری برای ارجاع هر مصرف‌کنندهٔ قدیمی به قالب جدید |
| `README.md` | مستندسازی رفتار دوگانهٔ URL سابسکریپشن |
| `test_subscription_integration.py` | آزمون browser page، feed کلاینت VPN، پاسخ API و flow رمز سابسکریپشن |

## آزمون

در ریشهٔ پروژه اجرا کنید:

```bash
python3 test_subscription_integration.py
```

> اسکریپت آزمون از دادهٔ نمونه و مسیر موقت استفاده می‌کند و برای تست محیط production نیازی به تغییر داده‌های واقعی ندارد.
